# preprocess.py
# Preprocessing utilities for trajectory data.
# Contains: normalize_trajectory(), add_velocity()
# - Agent-centric coordinate normalization (translate + rotate)
# - Velocity feature computation (dx, dy)
