# inference.py
# Generates submission predictions for the hackathon.
# Loads a trained checkpoint, runs inference on the test split,
# and outputs predicted trajectories in the required submission format.
