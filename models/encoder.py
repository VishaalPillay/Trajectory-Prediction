# encoder.py
# GRU Encoder for trajectory prediction.
# Processes 4-dimensional input (x, y, dx, dy) across 4 observed timesteps
# and outputs a fixed-size hidden state vector representing the agent's motion pattern.
